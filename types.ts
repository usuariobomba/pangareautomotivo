export interface ServiceItem {
  icon: string;
  title: string;
  description: string;
  items: string[];
}

export interface PartCategory {
  title: string;
  subtitle: string;
  image: string;
  tag: string;
  description: string;
}
